# Anthropic API key for Claude integration
ANTHROPIC_API_KEY=sk-ant-xxxxx

# (Optional) Firebase Admin credentials if using server-side Firestore
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
